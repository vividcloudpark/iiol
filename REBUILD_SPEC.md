# IIOL 재구축 요구사항 및 검증 기준

기준: 2026-09-24, 기존 저장소 `vividcloudpark/iiol`의 `4f3896180838c4527a4214a6e9de67b6dde3f4b1` 확인.

## 제품 목표

서점에서 책 뒷면의 바코드를 휴대폰으로 읽고 ISBN-13을 확인한 다음, 선택한 시·군·구에서 그 책을 소장한 도서관을 찾는다. 검색 결과에는 도서명, ISBN, 도서관, 소장 여부, 대출 가능 여부, 정보 기준 시점을 구분해 표시한다.

## 구현 상태

- 새 Django 앱, SQLite 기본 저장소, 지역 선택, 계정, ISBN 검색 API와 모바일 웹 화면을 만들었다.
- 카메라는 `BarcodeDetector` EAN-13 지원을 탐지하고, 지원되지 않거나 일정 시간 인식하지 못하면 ZXing 브라우저 디코더로 전환한다. 사진과 직접 입력도 지원한다.
- 일반 상품 EAN-13과 검사 숫자가 맞지 않는 값은 거르고, 악보 식별용 `9790` 코드도 ISBN으로 받지 않는다.
- 비회원 검색, 가입·프로필 지역 필수, 전일 기준 대출 상태 표시를 구현했다.
- 서버 배포 및 Nginx Proxy Manager의 호스트 포트 `8018` 전달 안내는 [DEPLOYMENT.md](DEPLOYMENT.md)에 있다.
- 13개 Django 테스트, 4개 ISBN 프런트엔드 테스트, Vite 빌드, Compose 구성 검증을 통과했다.
- 정보나루 키가 아직 연결되지 않아 실제 API 응답은 검증하지 못했다. 실제 Android/iPhone 카메라 테스트도 실기기가 없어 보류 상태다.

## 확정된 정책

- 비회원도 검색할 수 있다. 비회원은 검색 전에 시·군·구를 선택한다.
- 회원가입에는 시·군·구 선택이 필수다. 가입 회원의 지역은 검색 기본값으로 쓰고, 검색 시 임시 변경할 수 있다.
- 회원은 프로필에서 본인 지역과 계정 정보를 수정할 수 있다. 지역을 비운 상태로 저장할 수 없다.
- GPS 권한은 필수 조건이 아니다. 현재 위치 기반 지역 추천은 이후 선택 기능으로 검토한다.

## 핵심 사용자 흐름

1. 첫 화면에서 지역을 확인하거나 선택한다.
2. `바코드 스캔`을 누르면 카메라 권한을 요청하고 후면 카메라를 우선 사용한다.
3. EAN-13 결과 중 유효한 ISBN-13만 채택한다. 인식한 번호와 도서를 화면에 보여주고 사용자가 확인할 수 있게 한다.
4. 카메라를 쓸 수 없거나 인식이 실패하면 `사진 촬영/선택`과 `ISBN 직접 입력`으로 진행한다.
5. 선택한 지역에서 소장 도서관을 검색한다. 조회 실패, 미소장, 대출 불가를 서로 다른 상태로 표시한다.
6. 검색 결과에서 회원은 지역을 변경하거나 프로필의 기본 지역을 수정할 수 있다.

## 구현 요구사항

### ISBN 인식

- 웹 표준 `getUserMedia()`로 카메라 스트림을 받고 `facingMode: "environment"`를 우선 요청한다. 영상은 `muted playsinline`으로 표시하고, 페이지 이탈 또는 스캔 종료 시 트랙을 정지한다. 카메라는 HTTPS 배포 환경에서 검증한다.
- 브라우저에 `BarcodeDetector`가 있고 `getSupportedFormats()`에 `ean_13`이 있으면 사용한다. 지원되지 않으면 EAN-13을 읽는 브라우저용 디코더로 대체한다. 기능 탐지 결과만으로 성공을 가정하지 않고 실기기 테스트를 통과해야 한다.
- 사진 입력은 `accept="image/*"`와 후면 카메라 힌트 `capture="environment"`를 제공한다. 사진 선택과 직접 입력은 카메라 권한 거부에도 접근 가능해야 한다.
- 스캔 결과와 직접 입력 모두 숫자 13자리, `978`/`979` 접두어, ISBN-13 검사 숫자를 검증한다. `9790` 등 ISBN이 아닌 코드의 처리 범위를 ISBN 기관 기준으로 검토한다. 부가 가격 코드나 일반 상품 EAN-13은 ISBN으로 취급하지 않는다.
- 연속 프레임에서 같은 코드가 반복 검출되어도 검색은 한 번만 시작한다. 둘 이상의 코드가 보이면 유효한 ISBN을 선택하거나 재스캔을 요청한다.

### 도서관 검색

- 도서관 정보나루 인증키는 서버에 보관한다. `srchDtlList`로 도서 정보를, `libSrchByBook`으로 지역 내 소장 도서관을 조회하는 구성을 우선 검증한다. 필요한 도서관에 `bookExist`를 호출해 대출 가능 여부를 붙인다.
- `bookExist`의 대출 가능 정보는 공식 안내상 **조회일 기준 전날 상태**다. 결과 화면에 `전일 기준`을 표시하고 실시간 대출 가능으로 표현하지 않는다.
- 데이터 제공에 참여하지 않는 도서관이 있으므로 `검색 결과 없음`을 `해당 지역 모든 도서관에 없음`으로 단정하지 않는다.
- API 오류, 타임아웃, 인증키 문제, 결과 없음은 별도로 처리한다. 호출 제한과 페이지네이션, 캐시 만료 정책은 발급받은 키와 실제 응답으로 검증한다.
- 지역 목록은 정보나루 코드와 동기화할 수 있어야 한다. 오래된 지역명과 행정구역 변경을 점검하고, 회원의 지역 코드가 바뀌어도 계정이 삭제되지 않도록 설계한다.

### 계정

- 가입, 로그인, 로그아웃, 프로필 조회/수정, 비밀번호 변경을 제공한다.
- 가입과 수정 요청 모두 서버에서 지역 코드의 존재 여부를 검증한다. 이메일의 중복 정책, 인증/복구 방식, 세션 만료 처리는 구현 전에 결정한다.
- 회원의 지역 저장은 개인정보 최소화 원칙에 따라 시·군·구 코드로 제한한다. GPS 좌표는 기본 수집·저장하지 않는다.

## 기존 코드 점검 결과

- 현 바코드 화면은 `image/*` 업로드 후 서버 OpenCV로 분석한다. 실시간 카메라 스캔은 주석 처리되어 있다.
- `barcode.jpg`를 OpenCV 4.8로 해독하면 `1923055034006`이 나온다. 13자리 EAN-13이지만 ISBN 접두어가 아니다. 현 코드에는 ISBN 접두어·검사 숫자 검증이 없어 잘못된 검색으로 진행할 수 있다.
- 수기 입력 길이가 13이 아니어도 400 응답을 `return`하지 않아 처리 흐름이 계속된다.
- `my_region`은 DB에서 `null=True, blank=True`이고 가입·수정 폼에서도 필수가 아니다.
- `libSrch` 호출은 `pageSize=20` 한 페이지만 읽고, 각 도서관의 `bookExist`를 순서대로 호출한다. 지역 도서관 누락과 지연 가능성이 있다.
- 지역 JSON에는 `강원도`, `전라북도`, 경북 `군위군` 등 변경 전 표기가 남아 있다.
- 프런트엔드 `App.test.js`는 기본 문구를 찾는 초기 예제이며 실제 화면과 맞지 않는다. `frontend/package.json`이 없어 프런트엔드 테스트를 재현할 수 없다. Django 테스트는 0개다.

## 이번 점검에서 실행한 검증

- `python3 -m compileall -q iiol`: 통과. Python 파일 구문 오류는 발견되지 않았다.
- 테스트용 환경 변수로 `python3 manage.py check`: 통과. Django 시스템 점검에서 오류는 발견되지 않았다.
- 같은 환경에서 `python3 manage.py test --verbosity 1`: 테스트 0개 발견 및 실행. 기능 품질에 대한 근거로 사용할 수 없다.
- `cv2.barcode_BarcodeDetector().detectAndDecode()`로 저장소의 `iiol/barcode/barcode.jpg` 분석: `1923055034006` 검출. 유효한 EAN-13이지만 ISBN 접두어는 아니다.
- 이 환경에는 Android/iPhone 실기기와 카메라 입력 장치가 없어 모바일 카메라·브라우저 동작은 실행 검증하지 못했다.

## 테스트 계획과 통과 기준

| 범위 | 필수 사례 | 통과 기준 |
| --- | --- | --- |
| ISBN 단위 테스트 | 유효한 978/979, 검사 숫자 오류, 13자리 일반 EAN, 12/14자리, 공백·하이픈, 부가 코드 | 유효 ISBN만 검색 단계로 이동 |
| 카메라 기능 | 권한 허용·거부, 후면 카메라 없음, HTTPS, 탭 전환, 재진입 | 정상 스캔 또는 사진/직접 입력으로 복구; 카메라 트랙 정리 |
| 실기기 스캔 | Android Chrome·Samsung Internet, iPhone Safari·Chrome; 선명함·반사·기울기·어두움·복수 바코드·가격 라벨 | 각 기기에서 정상 조명 책 20권 중 19권 이상 10초 안에 인식, 잘못된 ISBN 확정 0건; 실패 사례 기록 |
| 도서관 API | 책 있음/없음, 지역 내 결과 없음, API 실패·지연, 페이지 초과, 전일 대출 상태 | 결과 상태와 기준 시점이 정확히 표시됨 |
| 회원/지역 | 가입 시 미선택, 유효 지역, 프로필 지역 변경, 비회원 검색, 비회원→가입 | 필수 지역이 서버에서 강제되고 기본값·임시 변경이 올바름 |

실기기 테스트는 HTTPS로 접근 가능한 시험 환경과 실제 Android/iPhone이 준비된 후 수행한다. 자동화 테스트만으로 카메라 초점, 조명, 브라우저 권한 UI의 품질을 보증할 수 없다.

## 착수 전 준비

1. 도서관 정보나루 Open API 키와 시험 가능한 HTTPS 도메인.
2. 테스트용 Android와 iPhone 기기, 브라우저/OS 버전 기록, ISBN 바코드가 있는 실물 책 20권 이상.
3. 계정 이메일 인증·복구 정책과 검색 기록 보관 여부 확정.
4. 새 코드베이스에서 API 응답 예시를 고정한 계약 테스트와 ISBN 검사 단위 테스트부터 작성.

## 근거 자료

- [MDN: getUserMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia), [BarcodeDetector](https://developer.mozilla.org/en-US/docs/Web/API/BarcodeDetector), [capture 속성](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/capture)
- [국제 ISBN 기관: ISBN 구조와 검사 숫자](https://www.isbn-international.org/index.php/node/10)
- [도서관 정보나루: Open API 활용방법](https://data4library.kr/apiUtilization), [대출 가능 정보의 전일 기준 안내](https://data4library.kr/noticeV?cn=129)

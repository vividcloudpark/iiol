# PROJECT KNOWLEDGE - IsInOurLib? (IIOL)

이 문서는 IIOL(이올) 프로젝트의 핵심 도메인 지식, 외부 API 연동 방식, 비즈니스 모델(BM), 기술적 아키텍처 전략 등을 정리한 프로젝트 지식 베이스입니다.

---

## 📖 1. 프로젝트 개요 & 비즈니스 도메인
IIOL은 **"이 책은, 우리 동네 도서관에 소장되어 있고 대출이 가능할까?"** 라는 단순하지만 명확한 질문에서 출발했습니다. 
서점에서 책을 즉흥적으로 구매하기엔 부담스럽고, 근처 도서관 홈페이지에서 일일이 책 제목을 검색하는 행위가 번거로운 유저층을 타겟팅합니다.

### 💡 핵심 가치 제안 (Value Proposition)
1.  **ISBN 바코드 인식 기반 검색**: 책의 바코드를 모바일 카메라로 스캔하여 즉시 도서 정보를 검색합니다.
2.  **내 주변 도서관 실시간 매핑**: 사용자가 지정한 관심 지역의 공공도서관들을 대상으로 실시간 소장 및 대출 가능 여부를 대조합니다.
3.  **Book-Todo (독서 위시리스트)**: 읽고 싶은 도서를 저장 시점의 사유 및 카테고리와 함께 저장하여 개인 독서 목표를 관리합니다.

### 💰 비즈니스 모델 (BM)
*   **지역별 독서 패턴 & 인기도서 데이터 비즈니스**: 대출이 빈번한 도서 및 대출되지 않은 가망 인기 도서의 데이터를 공공도서관, 출판사, 온·오프라인 서점 등에 마케팅 분석 자료(VMD 강화용)로 공급합니다.
*   **서점 및 E-book 구독 서비스 레퍼럴**: 도서가 근처 도서관에 없거나 대출 중일 경우, 교보문고, 알라딘, Yes24 및 밀리의 서재 등과의 제휴 API를 통해 즉시 구매/구독으로 연결하는 레퍼럴 수익 모델을 적용합니다.

---

## 🔌 2. 외부 API 아키텍처 (도서관정보나루)
프로젝트는 국립중앙도서관 도서관빅데이터 분석 플랫폼인 **도서관정보나루 (data4library.kr)** 의 OpenAPI를 핵심 엔진으로 삼습니다.

### 🛰️ 주요 연동 API 및 상세

#### **A. 도서 상세조회 API (Book Detail)**
*   **호출 목적**: 책의 ISBN13 코드를 기반으로 책의 메타데이터(제목, 저자, 출판사, 책 이미지)와 기본 인구통계학적 분석 통계(성별, 연령대별 선호도)를 획득합니다.
*   **요청 파라미터**: `isbn13`, `authKey`
*   **응답 데이터**: 도서 상세 정보, 대출 실적 통계 등.

#### **B. 도서별 이용분석 API (Book Analysis)**
*   **호출 목적**: 해당 도서와 연관하여 많이 대출된 도서 목록 및 다대출 이용자 그룹 분석 정보를 조회해 추천 서비스를 구현합니다.
*   **요청 파라미터**: `isbn13`, `authKey`
*   **응답 데이터**: 함께 대출된 도서(Co-loan) 목록, 연령별 대출 비중 등.

#### **C. 도서관별 도서 소장여부 및 대출가능여부 조회 API (Library Book Status)**
*   **호출 목적**: 선택된 지역의 특정 도서관 코드들에 대해 실시간으로 소장 여부(`hasBook`) 및 대출 가능 여부(`loanAvailable`)를 확인합니다.
*   **요청 파라미터**: `libCode`, `isbn13`, `authKey`
*   **응답 데이터**: `result` (소장 여부 Y/N, 대출가능 여부 Y/N 등)

---

## ⚡ 3. 핵심 기술 해결 전략

### 📸 A. 바코드(ISBN13) 인식 방식 최적화 (Server -> Client 이관)
*   **기존**: 사용자가 책 바코드를 사진 촬영하여 업로드하면 서버에서 `OpenCV` 및 `pyzbar` 라이브러리를 사용해 바코드를 추출하고 디코딩했습니다. 이 방식은 고용량 이미지 전송으로 인한 네트워크 지연과 서버 자원 낭비가 컸습니다.
*   **개선**: 프론트엔드(React/TypeScript)에 `html5-qrcode` 또는 `jsQR`과 같은 클라이언트 사이드 바코드 스캔 라이브러리를 도입합니다. 모바일 브라우저의 WebRTC 비디오 스트림을 활용해 클라이언트 기기에서 실시간 바코드 디코딩을 수행하고, 추출된 `ISBN13` 텍스트만 서버로 전송합니다. 서버 부하를 90% 이상 절감하고 사용자 경험을 비약적으로 향상시킵니다.

### 🚀 B. 캐싱 및 속도 향상 전략 (Redis)
*   도서관정보나루 API는 대용량 트래픽 상황에서 높은 응답 시간(Latency)을 보일 수 있습니다.
*   **Redis 캐싱 레이어**:
    *   도서 메타데이터(도서 상세 및 통계)는 변경이 드물기 때문에 Redis에 1일(24시간) 동안 캐싱합니다.
    *   특정 도서관의 실시간 대출 상태는 변동성이 있으므로 짧은 캐시 타임아웃(예: 30분 ~ 1시간)을 지정하여 무분별한 OpenAPI 호출을 줄이고 응답 속도를 **1.5초 이내**로 방어합니다.

### 📝 C. 데이터 모델 설계 (PostgreSQL)
1.  **SearchLog (검색 로그)**:
    *   검색된 ISBN, 검색 지역, 대상 도서관, 검색 일시, 로그인 유저 매핑 정보 저장.
    *   비즈니스 모델(BM)의 기초 통계 데이터로 활용.
2.  **BookWishlist (Book-Todo)**:
    *   유저 ID, 도서 ISBN13, 도서명, 저자, 이미지 URL, 저장 시점, 위시리스트 저장 사유(예: "공부 목적", "추천 받음" 등), 읽음 상태 필드 관리.

---

## 🛠️ 4. 기술 스택 마이그레이션 요약

| 구분 | 레거시 스택 | 모던화 스택 (변경 후) | 비고 |
| :--- | :--- | :--- | :--- |
| **Backend** | Django 4.2 | **Django 5.1** | 최신 ORM 성능 향상 및 신기능 적용 |
| **API Doc** | drf-yasg (Swagger) | **drf-spectacular (OpenAPI 3.0)** | Django 5.x 호환성 강화 및 OpenAPI 표준 준수 |
| **DB / Cache** | PostgreSQL / Redis | **PostgreSQL / Redis** | 캐싱 타임아웃 및 성능 튜닝 수행 |
| **Frontend** | CRA (JavaScript) | **Vite + React 18 + TypeScript** | 빠른 빌드 타임, 타입 가드, 모던 컴포넌트 설계 |
| **Styling** | Vanilla CSS (기본형) | **Premium Vanilla CSS (CSS Modules)** | 다크모드, Glassmorphism, 모던 반응형 레이아웃 구현 |

---

## 🐳 5. 인프라 컨테이너 구성 및 환경별 배포 전략 (Docker & .env)

로컬 개발 환경과 실제 라즈베리파이(Raspberry Pi) 배포 환경 간의 기술적 연계 및 격리 설계입니다.

### 📦 A. 컨테이너 서비스 구성 (Docker Compose)
데이터베이스 및 캐시 시스템 인프라를 가상화하여 호스트 OS에 종속되지 않고 언제든지 띄우고 끌 수 있도록 구성합니다.
*   **PostgreSQL 15 (iiol-db)**: 사용자 데이터, 검색 로그 및 독서 위시리스트 데이터를 영구 마운트 볼륨(`pgdata`)에 보관합니다.
*   **Redis 7 (iiol-redis)**: 도서관 실시간 API 응답 캐싱 및 Celery 비동기 작업 큐로 활용하며 볼륨(`redisdata`)을 마운트하여 데이터를 유지합니다.

### ⚙️ B. 환경 변수 파일 (.env) 기반 아키텍처 격리
라즈베리파이 프로덕션 배포 시 소스 코드 수정 없이 환경설정을 다르게 구성할 수 있도록 지원합니다.
*   **로컬 개발 환경**: `TARGET_ENV=dev`, `DEBUG=True`, `POSTGRES_HOST=127.0.0.1`, `REDIS_LOCATION=redis://127.0.0.1:6379/0` 로컬 호스트 포트에 바인딩하여 띄운 프로세스(Django, Vite)가 직접 접근합니다.
*   **라즈베리파이 운영 환경**: `TARGET_ENV=prod`, `DEBUG=False`, `POSTGRES_HOST=db`, `REDIS_LOCATION=redis://redis:6379/0` 컨테이너 네트워크 브릿지를 통해 컨테이너명(db, redis)으로 통신을 격리하고 보안을 강화합니다.

---

## 🪵 6. 로깅 가이드라인 (Logging Standards)

개발 및 운영 환경에서 시스템 모니터링과 디버깅 편의성을 높이기 위해 엄격한 로깅 표준을 준수합니다.

### 🔍 현재 로그 확인 방식
- **컨테이너 통합 로그**: Docker Compose를 통해 구동 시 백엔드의 표준 출력(stdout/stderr)이 도커 로그에 집계됩니다.
  - 실행 명령어: `docker compose logs backend` (실시간 로그 추적: `-f` 옵션 추가)
- **외부 API 호출 로그**: `LibraryApi` 호출 시 `API CALLED ->>> [서비스명]` 포맷의 출력문이 남습니다.
- **검색 결과 데이터베이스 영구 로그**: 사용자의 모든 검색 결과와 응답 성공 여부, 지연 시간 등은 DB 내의 `Barcode` 테이블에 트래킹 목적으로 자동 누적 기록됩니다.

### 🚨 향후 구현 시 필수 지침 (Best Practices)
1. **표준 라이브러리 사용**: 단순 `print()` 호출을 지양하고, Python 표준 `logging` 패키지(Django 통합 logger)를 활용하여 정해진 로그 레벨(`INFO`, `WARNING`, `ERROR`, `CRITICAL`)에 따라 메시지를 구분 출력해야 합니다.
2. **구조화된 로그 데이터 (JSON)**: 로그 트레이싱 및 통합 수집을 용이하게 하기 위해, 장기적으로 JSON 포맷팅이나 정형화된 메타데이터를 로그 내용에 포함하도록 유도합니다.
3. **중요 예외 처리 내역 기록**: 외부 OpenAPI 연동 시 발생하는 타임아웃, 포맷 에러, 빈 응답 등은 단순 `except: pass`나 `print(e)` 처리에 그치지 않고, 반드시 `logger.error` 혹은 `logger.warning` 블록을 통하여 원인 추적이 가능한 Context를 로깅해야 합니다.

